#include "monty.h"
/**
 * handle_nop - Remove the top element of the stack.
 * @head: Pointer to the top of the stack.
 * @line_number: Line number in the script where the opcode was found.
 */
void handle_nop(stack_t **head, unsigned int line_number)
{
	(void)line_number;
	(void)head;
}
